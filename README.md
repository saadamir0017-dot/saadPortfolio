# Saad Amer Portfolio

Static site (HTML/CSS/JS). Must be served over HTTP because it `fetch()`es JSON.

## Run

```bash
npx serve .
```

or

```bash
python3 -m http.server 8080
```

Then open the printed `http://localhost:...` URL.
